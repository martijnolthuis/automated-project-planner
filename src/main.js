// Import necessary functions from Vue 3.
import { createApp } from 'vue';

// Import the main App component, which will act as the root for our Vue instance.
import App from './App.vue';

// Create a new Vue application instance using the App component.
// Then, mount this application to an HTML element with the id of 'app'.
createApp(App).mount('#app');
